# AWS Notes

This folder will hold AWS-specific infrastructure assets for the new project.

Initial targets:

- object storage
- async jobs
- container deployment
- metadata database

Design rule:

Everything here should plug into portable interfaces defined outside the infrastructure layer.

